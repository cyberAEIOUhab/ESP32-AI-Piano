"""
好的,测试清单要覆盖板载核心外设,按照"从简单到复杂"的顺序排列,方便你逐步排查问题。

## 外设测试清单(第一周)

### 测试前置检查
- [ ] 确认J11两个引脚已连接(跳线帽/短接线),否则蜂鸣器测试会失败
- [ ] 确认ESP32已烧录MicroPython固件(之前用esptool操作的那一步)
- [ ] 确认Thonny能正常连接COM3,能进入REPL交互界面

---

### 测试1:LED2(绿灯,GPIO32)—— 最简单,优先测
- **预期现象**:代码让GPIO32输出低电平时,LED2点亮;输出高电平时熄灭(低电平有效)
- **验证点**:确认"低电平点亮"这个此前从原理图推断的结论是否与实测一致

### 测试2:LED3(红灯,GPIO33)
- 逻辑与LED2完全一致,重复验证一次确保两颗LED电路一致

### 测试3:KEY1、KEY2(按键输入,GPIO34/35)
- **预期现象**:通过`print()`在REPL里实时打印电平变化,按下按键时电平应该有明确翻转
- **验证点**:
  - 确认按下/松开时电平方向(是低有效还是高有效)
  - 确认是否需要软件设置内部上拉/下拉,还是板子上已有硬件上拉电阻(K3/K4旁边VCC那一路暗示可能已有硬件上拉)
  - 顺便确认K3/K4究竟哪个是KEY1、哪个是KEY2(用来修正硬件分析表里发现的矛盾)

### 测试4:蜂鸣器(B1, GPIO25, PWM)
- **预期现象**:用`machine.PWM`在GPIO25输出不同频率,蜂鸣器应发出对应音高的声音
- **验证点**:
  - 确认J11必须短接,否则完全不响(此前已通过你的实测确认)
  - 测试几个不同频率(比如262Hz、294Hz等do-re-mi频率),确认音高有区分度
  - 确认duty(占空比)设置在什么范围声音最清晰

### 测试5(可选,组合测试):按键联动LED+蜂鸣器
- **预期现象**:按下KEY1时,同时点亮LED、播放一个音符,验证三者协同工作,为后续正式钢琴程序打基础
- 这一步不是必须的,但能提前发现"多个外设同时工作时是否有相互干扰"的问题

---

## 每项测试建议记录的内容

对每一项,建议记录:
1. 测试代码片段(简短)
2. 实际现象(是否符合预期)
3. 如果不符合预期,做了什么调整、最终结论是什么

这些记录直接可以用作:硬件分析文档的补充证据 + 第一周视频里"真实硬件演示"的素材。

"""

测试基本完成

测试结果/问题已经体现在代码注释中


